Finds routes for an Everest Roam challenge (min 10000 vertical meters, 400 km). Brute force currenly.

Usage: ./EverestRoam <graph file> <start vertex name>

There's an example graph file in example_data.